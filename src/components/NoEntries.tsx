import { Card, Typography } from '@mui/material';
import React from 'react';

export const NoEntries: React.FC = () => {
    return (
        <Card>
            <Typography>No entries yet</Typography>
        </Card>
    )
}